# Artifacts

This folder contains generated outputs from training and evaluation.

## Structure
- `models/` - model checkpoints (tracked with Git LFS)
- `metrics/` - structured run metadata and numeric logs
- `figures/` - plots used in reports/presentations

## Notes
- `models/checkpoint_latest.pt` is stored via Git LFS.
- Keep only essential artifacts here to avoid repo bloat.
